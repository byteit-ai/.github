# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#  Copy this file to each Python repo:
#    .github/workflows/tests-unit.yml
#
#  Purpose:
#  - Run unit tests only (tests/unit) in PRs and pushes to dev/main
#  - Delegate failure Jira comments/rollback to AdolfoBOT in reusable workflow
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
name: Pytest Unit

on:
  pull_request:
    branches: [main]
    paths:
      - "tests/**"
      - "src/**"
      - "requirements*.txt"
      - "pyproject.toml"
      - "pytest.ini"
  push:
    branches: [main]
    paths:
      - "tests/**"
      - "src/**"
      - "requirements*.txt"
      - "pyproject.toml"
      - "pytest.ini"

concurrency:
  group: tests-unit-${{ github.ref }}
  cancel-in-progress: true

jobs:
  tests-unit:
    permissions:
      contents: read
    uses: byteit-ai/.github/.github/workflows/tests-unit.yml@main
    with:
      python_version: "3.12"
    secrets: inherit
